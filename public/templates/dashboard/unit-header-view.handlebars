<td colspan="4">
	<a href="deploy-unit" data-bypass="1" class="deploy-unit-link">{{name}}</a>
</td>
<td>
	<button class="btn-hover btn-select {{#if allSelected}}selected{{/if}}">
		<i class="icon-white icon-ok"></i>
	</button>
</td>
<td colspan="3" style="width: 100%">
	{{#if showActions}}
		<button class="btn btn-info btn-select-version" style="margin-right: 30px">
			<icon class="icon-download-alt icon-white" />
			Deploy
		</button>

		{{#each actions}}
			<button class="btn btn-info btn-unit-action" data-action-name="{{this}}">
				<icon class="{{iconForUnitAction this}} icon-white" />
				{{this}}
			</button>
		{{/each}}

		{{#if showLoadBalancerToggle}}
			<span class="load-balancer-actions" style="margin-left: 30px">
				<button class="btn btn-success btn-set-loadbalancer-in">
					<icon class="icon-share2" />
					<span style="padding-left: 3px">In</span>
				</button>
			</span>
			<span class="load-balancer-actions">
				<button class="btn btn-success btn-set-loadbalancer-out">
					<icon class="icon-share2" />
					<span style="padding-left: 3px">Out</span>
				</button>
			</span>
		{{/if}}
	{{/if}}
</td>
